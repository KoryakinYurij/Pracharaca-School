# Palette’s Journal

- **Semantic Heading Hierarchy**: `QACard` questions use `<h3>` to wrap the accordion button. Inside the answer content, `AnswerSection` titles should use `<h4>` to maintain a correct document outline and allow screen reader navigation between answer parts.
